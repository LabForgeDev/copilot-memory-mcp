# Sentence Transformers model cache directory
# Defaults to /app/models inside the Docker image
SENTENCE_TRANSFORMERS_HOME=/app/models

# ChromaDB persistence path
# Defaults to /data/chroma inside the Docker container
CHROMA_PATH=/data/chroma

# Server port (default: 8000)
PORT=8000
